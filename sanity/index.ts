import { page, heroSection, servicesSection, aboutSection, ctaSection, faqSection } from "./page";
import { service } from "./service";
import { category } from "./category";
import { galleryImage } from "./galleryImage";
import { settings } from "./settings";
import { review } from "./review";

export const schemaTypes = [
  page, heroSection, servicesSection, aboutSection, ctaSection, faqSection,
  service, category, galleryImage, settings, review,
];
